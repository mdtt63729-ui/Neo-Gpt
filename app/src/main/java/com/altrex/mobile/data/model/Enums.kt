package com.altrex.mobile.data.model

enum class Mode { ASK, AGENT, MULTI }

enum class TaskStatus { QUEUED, WAITING, RUNNING, VERIFYING, COMPLETED, FAILED, BLOCKED, CANCELLED }

enum class RunStatus { PLANNING, RUNNING, VERIFYING, INTEGRATING, COMPLETED, FAILED, CANCELLED, INTERRUPTED }

enum class ProviderHealthState { HEALTHY, DEGRADED, RATE_LIMITED, QUOTA_EXHAUSTED, OFFLINE }

enum class ConnectionState {
    NOT_CONFIGURED, TESTING, CONNECTED, RATE_LIMITED, QUOTA_EXHAUSTED,
    AUTHENTICATION_FAILED, MODEL_UNAVAILABLE, TEMPORARILY_UNAVAILABLE, ERROR
}

enum class ProviderErrorCategory {
    AUTH_ERROR, INVALID_API_KEY, MODEL_NOT_FOUND, MODEL_UNAVAILABLE,
    RATE_LIMITED, QUOTA_EXHAUSTED, BAD_REQUEST, TOOLS_UNSUPPORTED,
    CONTEXT_TOO_LARGE, TIMEOUT, CONNECTION_ERROR, PROVIDER_SERVER_ERROR,
    CANCELLED, UNKNOWN
}

enum class FailureKind {
    AUTHENTICATION, TOO_LARGE, RATE_LIMIT, QUOTA_EXHAUSTED, TIMEOUT,
    UNAVAILABLE, MODEL_UNAVAILABLE, TOOLS_UNSUPPORTED, INVALID_REQUEST,
    NETWORK, CANCELLED
}

enum class ProviderSection { RECOMMENDED, LOCAL, ADDITIONAL, ADVANCED }

enum class MessageStatus { COMPLETE, STREAMING, ERROR, CANCELLED }

enum class ChatStreamEventType {
    STARTED, DELTA, ACTIVITY, FILES_CHANGED, COMMAND_RESULT,
    COMPLETED, CANCELLED, ERROR, RUN_STATE
}
